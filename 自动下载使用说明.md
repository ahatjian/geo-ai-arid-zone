# 🛰️ 卫星数据自动下载工具使用说明

## 📦 文件说明

| 文件 | 说明 |
|------|------|
| `config.py` | 配置文件（修改账号、研究区等参数） |
| `download_planetary.py` | **推荐！** Planetary Computer 下载（无需注册） |
| `download_landsat.py` | USGS Earth Explorer 下载（Landsat） |
| `download_sentinel.py` | Copernicus Hub 下载（Sentinel-2） |
| `download_all.py` | 统一入口，选择下载源 |

---

## 🚀 快速开始（3步搞定）

### 第1步：修改配置文件

打开 `config.py`，修改以下参数：

```python
# 研究区（从预设中选择或自定义）
STUDY_AREA_NAME = "柴达木盆地"   # 修改为你需要的区域
BBOX = [94, 36, 98, 39]          # [最小经度, 最小纬度, 最大经度, 最大纬度]

# 时间范围
START_DATE = "2020-01-01"        # 开始日期
END_DATE = "2024-12-31"          # 结束日期

# 云量限制
MAX_CLOUD_COVER = 20             # 最大云量 %

# 下载目录（重要！修改为你硬盘的路径）
DOWNLOAD_DIR = "E:/卫星数据"      # 默认是当前目录下的 data/ 文件夹
```

**如果需要下载到移动硬盘**，把路径改成：
```python
DOWNLOAD_DIR = "E:/卫星数据"  # 假设移动硬盘是 E: 盘
```

### 第2步：运行下载脚本

打开终端，进入项目目录：

```bash
cd c:\Users\AHATJIAN\WorkBuddy\20260421131650\web-geo-ai
```

然后运行：

```bash
python download_all.py
```

### 第3步：选择下载源

程序会显示菜单，输入数字选择：

```
[1] Planetary Computer (推荐) - 无需注册
[2] USGS Earth Explorer - 需要账号
[3] Copernicus Hub - 需要账号
```

**推荐选 [1]**，最简单！

---

## 📊 预估数据量

根据你的设置，数据量大致如下：

| 下载内容 | 单景大小 | 50景总量 | 100景总量 |
|---------|---------|---------|----------|
| RGB预览图 (30%分辨率) | ~50 MB | ~2.5 GB | ~5 GB |
| RGB全分辨率 | ~500 MB | ~25 GB | ~50 GB |
| 所有波段 (Sentinel-2) | ~1 GB | ~50 GB | ~100 GB |
| 所有波段 (Landsat) | ~300 MB | ~15 GB | ~30 GB |

**建议**：
- **先试 RGB 预览图**（选模式1），看看数据质量
- 确认好用后再下载完整波段（选模式2）

---

## 🔧 高级配置

### 修改同时下载数量

在 `config.py` 中：
```python
MAX_CONCURRENT_DOWNLOADS = 3   # 同时下载3个文件
```

### 只下载特定卫星

在 `config.py` 中：
```python
DOWNLOAD_LANDSAT8 = True    # True=下载, False=跳过
DOWNLOAD_LANDSAT9 = True
DOWNLOAD_SENTINEL2 = True
```

### 限制下载数量

```python
MAX_SCENES_PER_SATELLITE = 50   # 每颗卫星最多下载50景
```

---

## ⚠️ 注意事项

### 1. 硬盘空间
下载前请确保硬盘有足够空间：
- **预览图模式**：预留 10-50 GB
- **完整波段模式**：预留 100 GB - 2 TB

### 2. 网络稳定
- 下载过程中请勿断网
- 如中断可重新运行脚本，已下载的文件会自动跳过

### 3. 下载时间
- 预览图：每景 1-5 分钟
- 完整波段：每景 10-30 分钟
- 50景完整数据可能需要 **10-20 小时**

建议：
- 晚上挂机下载
- 或分批下载（修改时间范围）

### 4. 断点续传
如果下载中断，直接重新运行脚本即可：
- 已完成的文件会自动跳过
- 只下载未完成的文件

---

## 📁 下载后的文件结构

```
E:/卫星数据/
└── 柴达木盆地/
    └── PlanetaryComputer/
        ├── 20240815_RGB.tif          # RGB 合成图（GeoTIFF）
        ├── 20240815_RGB_preview.jpg  # RGB 预览图（JPG）
        ├── 20240901_RGB.tif
        ├── 20240901_RGB_preview.jpg
        └── ...
```

如果选模式2（所有波段）：
```
E:/卫星数据/
└── 柴达木盆地/
    └── PlanetaryComputer/
        ├── 20240815/
        │   ├── B02.tif   # 蓝色波段
        │   ├── B03.tif   # 绿色波段
        │   ├── B04.tif   # 红色波段
        │   ├── B08.tif   # 近红外
        │   ├── B11.tif   # 短波红外1
        │   └── B12.tif   # 短波红外2
        ├── 20240901/
        └── ...
```

---

## 🎯 推荐配置（按研究目标）

### 配置A：快速预览（适合先看数据质量）
```python
STUDY_AREA_NAME = "柴达木盆地"
BBOX = [94, 36, 98, 39]
START_DATE = "2024-06-01"
END_DATE = "2024-09-30"
MAX_CLOUD_COVER = 30
MAX_SCENES_PER_SATELLITE = 10
DOWNLOAD_DIR = "E:/卫星数据"
```
预计：10景 × 50MB = **500 MB**

### 配置B：完整研究（一年数据）
```python
START_DATE = "2024-01-01"
END_DATE = "2024-12-31"
MAX_CLOUD_COVER = 20
MAX_SCENES_PER_SATELLITE = 50
```
预计：50景 × 1GB = **50 GB**

### 配置C：长时序分析（5年）
```python
START_DATE = "2020-01-01"
END_DATE = "2024-12-31"
MAX_CLOUD_COVER = 20
MAX_SCENES_PER_SATELLITE = 100
```
预计：200景 × 1GB = **200 GB**

---

## 💡 常见问题

**Q: 下载速度很慢怎么办？**
A: 
- 使用 Planetary Computer（国内可访问）
- 降低 `MAX_CONCURRENT_DOWNLOADS`（避免被限流）
- 晚上下载（网络更稳定）

**Q: 硬盘不够怎么办？**
A:
- 先用 "RGB预览图" 模式（文件小）
- 只下载需要的波段（修改脚本）
- 下载到外接硬盘

**Q: 下载失败了怎么重试？**
A:
- 直接重新运行脚本
- 已下载的文件会自动跳过
- 只下载失败的文件

**Q: 数据下载完了怎么用？**
A:
- GeoTIFF 文件可直接在 **ENVI/ArcGIS/QGIS** 中打开
- 也可以导入本应用（左侧选 "本地GeoTIFF文件"）

---

## 📞 遇到问题？

如果脚本运行出错：
1. 检查 `config.py` 参数是否正确
2. 检查硬盘空间是否足够
3. 检查网络连接
4. 把错误信息发给我，我帮你解决

**祝下载顺利！** 🛰️
